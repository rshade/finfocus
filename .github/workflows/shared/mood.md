# Mood

Shared workflow import placeholder for the GitHub Agentic Workflows (gh-aw) runtime.

This file is imported by workflow definitions via `imports:` and is required by
the `gh aw compile` lock file generation process. It serves as a runtime-import
dependency marker.

## Referenced By

- `docs-noob-tester.md` — imported as a shared dependency
