# Visual Assets Placeholders

The following screenshots need to be captured from the actual CLI application and replaced here.
The current files are text placeholders.

## Required Screenshots

1. **budget-tty-mode.png**: `finfocus cost projected` in a TTY (color, progress bars).
2. **budget-plain-mode.png**: `finfocus cost projected --plain`.
3. **recommendations-table.png**: `finfocus cost recommendations` interactive table.
4. **recommendations-detail.png**: Detail view of a recommendation.
5. **loading-spinner.gif**: Capture of the loading spinner.
6. **error-messages.png**: Example error output.

## Capture Instructions

See `contracts/screenshot-checklist.md` for guidelines.
Use `pngquant` to optimize PNGs (<500KB).
