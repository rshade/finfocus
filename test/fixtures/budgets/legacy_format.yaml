# Test fixture: Legacy single-budget format (for migration testing)
cost:
  budgets:
    amount: 5000.0
    currency: USD
    period: monthly
    alerts:
      - threshold: 80.0
        type: actual
      - threshold: 100.0
        type: forecasted
    exit_on_threshold: true
    exit_code: 1
